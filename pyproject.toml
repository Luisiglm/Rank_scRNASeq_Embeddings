[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sc_utils"
version = "0.1.0"
dependencies = [
    "scanpy",
    "matplotlib",
    "scikit-learn",
    "leidenalg",
    "igraph",
]

[tool.setuptools.packages.find]
where = ["src"]
